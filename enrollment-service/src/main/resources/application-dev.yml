spring:
  datasource:
    url: jdbc:mysql://172.17.0.1:3306/enrollment_db?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
    username: enrollment_user
    password: enrollment_pass
  cloud:
    nacos:
      discovery:
        server-addr: nacos:8848
        namespace: dev  # ✅ 使用 dev

# 使用服务名调用
user-service:
  url: http://user-service

catalog-service:
  url: http://catalog-service